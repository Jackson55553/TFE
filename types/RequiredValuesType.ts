export type RequiredValuesType = {
    name: string;
    symbol: string;
    supply: string;
    decimals: string;
};
export enum Required {
    name = 'name',
    symbol = 'symbol',
    decimals = 'decimals',
    supply = 'supply',
}
